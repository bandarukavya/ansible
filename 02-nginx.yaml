- name: install and run nginx
  hosts: web
  become: yes #equal to -b in adhoc cmds, getting root access
  tasks: 
  -name: install nginx
  ansible.builtin.dnf:
    name: nginx
    state: lates
  -name: start nginx
  ansible.builtin.service:
    name: nginx
    state: started 
    enabled: yes